# Override these if your machine cannot bind the default ports.
LOCAL_HTTP_PORT=80
LOCAL_HTTPS_PORT=443
LOCAL_DB_PORT=3307

# Keep this aligned with the HTTPS port when you switch away from 443.
LOCAL_BASE_URL=https://localhost
LOCAL_ALLOWED_ORIGINS=https://localhost,https://127.0.0.1
